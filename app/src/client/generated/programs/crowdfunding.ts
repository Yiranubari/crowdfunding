/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type ExtendedClient,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/program-client-core";
import {
  getCampaignCodec,
  type Campaign,
  type CampaignArgs,
} from "../accounts";
import {
  getCreateInstructionAsync,
  getDonateInstruction,
  getWithdrawInstruction,
  parseCreateInstruction,
  parseDonateInstruction,
  parseWithdrawInstruction,
  type CreateAsyncInput,
  type DonateInput,
  type ParsedCreateInstruction,
  type ParsedDonateInstruction,
  type ParsedWithdrawInstruction,
  type WithdrawInput,
} from "../instructions";
import { findCampaignPda } from "../pdas";

export const CROWDFUNDING_PROGRAM_ADDRESS =
  "7nambqXraRSemsidx7EBqxYQkhepXa3zV6TUvNuWWCuh" as Address<"7nambqXraRSemsidx7EBqxYQkhepXa3zV6TUvNuWWCuh">;

export enum CrowdfundingAccount {
  Campaign,
}

export function identifyCrowdfundingAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): CrowdfundingAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([50, 40, 49, 11, 157, 220, 229, 192]),
      ),
      0,
    )
  ) {
    return CrowdfundingAccount.Campaign;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    { accountData: data, programName: "crowdfunding" },
  );
}

export enum CrowdfundingInstruction {
  Create,
  Donate,
  Withdraw,
}

export function identifyCrowdfundingInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): CrowdfundingInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([24, 30, 200, 40, 5, 28, 7, 119]),
      ),
      0,
    )
  ) {
    return CrowdfundingInstruction.Create;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([121, 186, 218, 211, 73, 70, 196, 180]),
      ),
      0,
    )
  ) {
    return CrowdfundingInstruction.Donate;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([183, 18, 70, 156, 148, 109, 161, 34]),
      ),
      0,
    )
  ) {
    return CrowdfundingInstruction.Withdraw;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "crowdfunding" },
  );
}

export type ParsedCrowdfundingInstruction<
  TProgram extends string = "7nambqXraRSemsidx7EBqxYQkhepXa3zV6TUvNuWWCuh",
> =
  | ({
      instructionType: CrowdfundingInstruction.Create;
    } & ParsedCreateInstruction<TProgram>)
  | ({
      instructionType: CrowdfundingInstruction.Donate;
    } & ParsedDonateInstruction<TProgram>)
  | ({
      instructionType: CrowdfundingInstruction.Withdraw;
    } & ParsedWithdrawInstruction<TProgram>);

export function parseCrowdfundingInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedCrowdfundingInstruction<TProgram> {
  const instructionType = identifyCrowdfundingInstruction(instruction);
  switch (instructionType) {
    case CrowdfundingInstruction.Create: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: CrowdfundingInstruction.Create,
        ...parseCreateInstruction(instruction),
      };
    }
    case CrowdfundingInstruction.Donate: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: CrowdfundingInstruction.Donate,
        ...parseDonateInstruction(instruction),
      };
    }
    case CrowdfundingInstruction.Withdraw: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: CrowdfundingInstruction.Withdraw,
        ...parseWithdrawInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        {
          instructionType: instructionType as string,
          programName: "crowdfunding",
        },
      );
  }
}

export type CrowdfundingPlugin = {
  accounts: CrowdfundingPluginAccounts;
  instructions: CrowdfundingPluginInstructions;
  pdas: CrowdfundingPluginPdas;
  identifyAccount: typeof identifyCrowdfundingAccount;
  identifyInstruction: typeof identifyCrowdfundingInstruction;
  parseInstruction: typeof parseCrowdfundingInstruction;
};

export type CrowdfundingPluginAccounts = {
  campaign: ReturnType<typeof getCampaignCodec> &
    SelfFetchFunctions<CampaignArgs, Campaign>;
};

export type CrowdfundingPluginInstructions = {
  create: (
    input: CreateAsyncInput,
  ) => ReturnType<typeof getCreateInstructionAsync> & SelfPlanAndSendFunctions;
  donate: (
    input: DonateInput,
  ) => ReturnType<typeof getDonateInstruction> & SelfPlanAndSendFunctions;
  withdraw: (
    input: WithdrawInput,
  ) => ReturnType<typeof getWithdrawInstruction> & SelfPlanAndSendFunctions;
};

export type CrowdfundingPluginPdas = { campaign: typeof findCampaignPda };

export type CrowdfundingPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function crowdfundingProgram() {
  return <T extends CrowdfundingPluginRequirements>(
    client: T,
  ): ExtendedClient<T, { crowdfunding: CrowdfundingPlugin }> => {
    return extendClient(client, {
      crowdfunding: <CrowdfundingPlugin>{
        accounts: {
          campaign: addSelfFetchFunctions(client, getCampaignCodec()),
        },
        instructions: {
          create: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getCreateInstructionAsync(input),
            ),
          donate: (input) =>
            addSelfPlanAndSendFunctions(client, getDonateInstruction(input)),
          withdraw: (input) =>
            addSelfPlanAndSendFunctions(client, getWithdrawInstruction(input)),
        },
        pdas: { campaign: findCampaignPda },
        identifyAccount: identifyCrowdfundingAccount,
        identifyInstruction: identifyCrowdfundingInstruction,
        parseInstruction: parseCrowdfundingInstruction,
      },
    });
  };
}
